# Security Policy

Skrepka reads everything you copy. That makes a security report here worth more
than one against most apps of this size, and it is why this file says exactly
what is in scope rather than leaving you to guess.

## Supported versions

| Version | Supported |
| --- | --- |
| Latest release ([0.1.2](https://github.com/psoldunov/skrepka/releases/latest)) | ✅ |
| Any earlier release | ❌ |
| `master` | Best effort — fixes land here first |

Skrepka has no in-app updater, so `brew upgrade --cask skrepka` (or a fresh
download) is the whole patch path. There are no backports to older tags.

## Reporting a vulnerability

**Do not open a public issue.**

Use GitHub's private vulnerability reporting:

**[github.com/psoldunov/skrepka/security/advisories/new](https://github.com/psoldunov/skrepka/security/advisories/new)**

or go to the repository's **Security** tab and click **Report a vulnerability**.
The report is visible only to you and the maintainer until an advisory is
published. If GitHub is not an option for you, the contact details on
[soldunov.dev](https://www.soldunov.dev) reach the maintainer directly.

Please include:

- What an attacker gets, and what they need in order to get it — local code
  execution, an app the user already installed, physical access, nothing at all
- Steps to reproduce, ideally against a build from `master`
- The Skrepka version, macOS version, and whether the Mac is Apple silicon or
  Intel
- Any proof-of-concept, with your own clipboard contents redacted

### What to expect

This is a single-maintainer project with no bug bounty and no paid support.

- **Acknowledgement** within 7 days.
- **An assessment** — in scope or not, and a rough severity — within 14 days.
- **A fix released** as soon as it is ready. For anything that leaks clipboard
  contents, that means a point release rather than waiting for the next
  feature.
- **Credit** in the advisory and the release notes, unless you ask otherwise.

Please give the fix a reasonable window before disclosing publicly. If you
disagree with an assessment, say so in the advisory thread — that is a better
outcome than a silent disclosure.

## Threat model

Skrepka's job is to keep a local history and hand it back. It has **no network
code at all** — no telemetry, no sync, no update check. Nothing you copy leaves
the Mac unless you paste it somewhere.

History lives in a SwiftData store at
`~/Library/Application Support/dev.soldunov.skrepka/skrepka.store`, with large
image payloads in external files beside it. It is protected by macOS file
permissions and by FileVault if you have it on. **It is not separately
encrypted at rest.**

### In scope

- Recorded content that carried a privacy marker Skrepka honours —
  `org.nspasteboard.TransientType`, `org.nspasteboard.AutoGeneratedType` and
  the other rejected types in
  [`PrivacyMarkers.swift`](Sources/SkrepkaCore/Clipboard/PrivacyMarkers.swift)
  should never reach storage at all
- Concealed content (`org.nspasteboard.ConcealedType`, 1Password's own type)
  rendered in the clear anywhere in the UI — it is stored, and it is meant to
  be masked everywhere it is shown
- A copy from an app on the exclusion list being recorded anyway
- Clipboard contents reaching any file, log, crash report or process other than
  the store and the app itself
- A paste landing in an app other than the one that was frontmost, or the wrong
  entry being pasted
- Anything that makes Skrepka a lever for privilege escalation, particularly
  around its Accessibility grant
- Code-signing, notarization or Gatekeeper problems with a published build,
  including the Homebrew cask
- Path traversal, injection or memory-safety issues in decoding pasteboard
  payloads — a malicious app controls exactly what it puts on the pasteboard

### Not in scope

These are consequences of what a clipboard manager is, and reports about them
will be closed with a pointer to this section:

- **Another process running as your user can read the store.** macOS grants it
  that; no clipboard manager can prevent it. If that is your threat model, the
  answer is app sandboxing and FileVault, not this app.
- **The store is not encrypted at rest beyond FileVault.** Deliberate for now.
  A proposal for an encrypted store is a feature request, not a vulnerability.
- **Anyone with your unlocked Mac can open the picker.** By design — it is a
  keystroke away on purpose.
- **A password manager that sets no `org.nspasteboard.*` marker gets recorded.**
  Skrepka honours the markers that exist and offers a per-app exclusion list as
  the backstop. Tell the password manager's authors too.
- **Reports from automated scanners with no working proof of concept**, and
  anything requiring a macOS release older than 26, which Skrepka does not
  support.

## Building a trustworthy copy

Released builds are universal, signed with a Developer ID identity and
notarized by Apple, so they open with no Gatekeeper detour. If you would rather
not trust a binary, `scripts/notarize.sh` and `scripts/bundle.sh` build the
same thing from source — see [CONTRIBUTING.md](CONTRIBUTING.md).
